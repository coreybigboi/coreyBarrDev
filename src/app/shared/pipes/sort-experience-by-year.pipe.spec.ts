import { SortExperienceByYearPipe } from './sort-experience-by-year.pipe';

describe('SortExperienceByYearPipe', () => {
  it('create an instance', () => {
    const pipe = new SortExperienceByYearPipe();
    expect(pipe).toBeTruthy();
  });
});

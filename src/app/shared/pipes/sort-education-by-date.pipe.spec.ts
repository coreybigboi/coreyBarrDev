import { SortEducationByDatePipe } from './sort-education-by-date.pipe';

describe('SortEducationByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SortEducationByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
